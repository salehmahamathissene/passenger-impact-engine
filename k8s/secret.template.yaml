apiVersion: v1
kind: Secret
metadata:
  name: pie-secrets
  namespace: pie
type: Opaque
stringData:
  # Stripe (placeholders only — do NOT start with sk_/pk_)
  STRIPE_SECRET_KEY: "REPLACE_WITH_STRIPE_SECRET_KEY"
  STRIPE_PUBLIC_KEY: "REPLACE_WITH_STRIPE_PUBLIC_KEY"
  STRIPE_WEBHOOK_SECRET: "REPLACE_WITH_STRIPE_WEBHOOK_SECRET"

  # Auth
  JWT_SECRET: "REPLACE_WITH_LONG_RANDOM"

  # Infrastructure
  DATABASE_URL: "REPLACE_WITH_DATABASE_URL"
  REDIS_URL: "REPLACE_WITH_REDIS_URL"
